# ⚡ Game Bridge — интеграция Aiogram + FastAPI + Vue через Redis

Этот проект — пример связки **Telegram-бота (Aiogram)**, **Backend-сервера (FastAPI)** и **Frontend клиента (Vue)**  
через **Redis** как брокер сообщений.  
Архитектура спроектирована для высокой производительности и масштабирования под нагрузку (≈2000 игроков).

---

## 🧩 Архитектура

    ┌──────────────┐
    │   Telegram   │
    └──────┬───────┘
           │ Webhook
           ▼
    ┌──────────────┐
    │   Aiogram    │
    └──────┬───────┘
      POST /events
           │
           ▼
    ┌──────────────┐
    │   FastAPI    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │    Redis     │
    └──────┬───────┘
           │
    ┌──────┴──────────┐
    │ Vue (polling)   │
    └─────────────────┘

---

## ⚙️ Основная идея

- **Aiogram** получает апдейты от Telegram (через webhook)  
  и отправляет действия пользователей в **FastAPI** (`POST /events`).
- **FastAPI** обрабатывает игровую логику и отправляет события в Redis:
  - `vue:events` — очередь обновлений для клиента (Vue);
  - `telegram:responses` — очередь ответов для игроков.
- **Aiogram** периодически читает Redis и отправляет ответы игрокам.
- **Vue** делает polling (`GET /poll`) и показывает новые события в интерфейсе.

---

## 🧠 Поток данных

| Откуда | Куда | Тип связи | Описание |
|--------|------|------------|-----------|
| Telegram | Aiogram | Webhook | Получение апдейтов |
| Aiogram | FastAPI | HTTP POST | Отправка действий |
| FastAPI | Redis | rpush / lpop | Добавление событий |
| Aiogram | Redis | lpop | Чтение ответов для Telegram |
| Vue | FastAPI | HTTP polling | Чтение событий для клиента |

---

## 🛠️ Стек технологий

| Компонент | Технология |
|------------|-------------|
| Бот | [Aiogram 3.x](https://docs.aiogram.dev) |
| Сервер | [FastAPI](https://fastapi.tiangolo.com/) |
| Клиент | [Vue 3](https://vuejs.org/) |
| Брокер | [Redis](https://redis.io/) |
| Язык | Python 3.10+ |
